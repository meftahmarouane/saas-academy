import { ComingSoon } from "@/components/ui/ComingSoon";

export default function LaunchOverviewPage() {
    return (
        <ComingSoon
            title="Launch Module Overview"
            description="The overview dashboard for this module is under construction. You can still access the individual lessons via the Dashboard."
        />
    );
}
